# ── Node / Vite (Phase 1 additions) ─────────────────────────────────────────
node_modules/

# Vite build output — generated by GitHub Actions on every deploy.
# Do NOT commit this folder. Actions builds it fresh before Pages deployment.
dist/

# Vite dev server cache
.vite/

# ── Environment files ─────────────────────────────────────────────────────────
# .env.local holds VITE_MULTISET_API_KEY when the key arrives (Phase 2).
# Never commit API keys to the repository.
.env.local
.env.*.local

# ── OS / Editor ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/settings.json
.idea/
*.swp
